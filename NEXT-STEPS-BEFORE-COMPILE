[{'0.13", features = ["ecdsa': ''}, {'0.13", features = ["ecdsa", "serde': ''}, {'ecdsa': ''}, {'ecdsa", "serde': ''}, {'1.0", features = [/cserde = { version = "1.0", features = ["derive': ''}, ['u8; N]: serde::Serialize` errors**: \n   - Ensure serde is enabled with the "derive', 'feature (we\'ve done this)\n   - These errors should resolve with proper serde configuration\n\n2. **For `no field \'v\' on type \'ecdsa::Signature<Secp256k1>\'`**:\n   - In newer k256 versions, the recovery ID is accessed differently\n   - You may need to use `signature.recovery_id()` instead of `signature.v`\n\n3. **Type alias issues in error.rs**:\n   - Check that `pub type Result<T> = std::result::Result<T, NexusError>;` is correctly formatted\n\n### Step 3: Initialize and Run Testnet\n\nOnce compilation succeeds, follow the README instructions:\n\n```bash\n# Initialize data directory\n./target/release/nexus init --data-dir ~/.nexus --chain-id 1337\n\n# Run as non-validator (for testing)\n./target/release/nexus run --rpc-addr 127.0.0.1:8545\n\n# For validator node (requires key generation)\n./target/release/nexus generate-key --output ~/.nexus/validator.key\n./target/release/nexus run --validator --validator-key ~/.nexus/validator.key\n```\n\n### Step 4: Test the Node\n\nYou can test your node with:\n```bash\n# Check node info\ncurl -X POST -H "Content-Type: application/json" --data \'{"jsonrpc":"2.0', 'method":"web3_clientVersion', 'params', [], 'id":1}\' http://127.0.0.1:8545\n\n# Check block number\ncurl -X POST -H "Content-Type: application/json" --data \'{"jsonrpc":"2.0', 'method":"eth_blockNumber', 'params', [], 'id', 1]]